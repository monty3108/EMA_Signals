# My stock Portfolio View

## Process in File Operate
1. During initializing, it will check the format and null values in the file 'positions.csv'.
2. Once the initial check is passed, it will load 'positions.csv' file using 'load_data()' with already available index column as index.
3. 
